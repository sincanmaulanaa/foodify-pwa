/* eslint-disable implicit-arrow-linebreak */
const CONFIG = {
  BASE_URL: 'https://restaurant-api.dicoding.dev',
  BASE_IMAGE_URL: (size) =>
    `https://restaurant-api.dicoding.dev/images/${size}/`,
};

export default CONFIG;
